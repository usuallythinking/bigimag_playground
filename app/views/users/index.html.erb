<h1>All Users</h1>

<ul class="users">
	<% if @users %>
	<% @users.each do |user| %>
		<li>
			<%= link_to user.name, user %> signed up <%= user.created_at.to_date %>
		</li>
	<% end %>
		<% end %>

</ul>

